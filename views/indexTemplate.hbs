<!DOCTYPE html>
<html lang="en">
<body>
  <div>
    <form method="POST" action="/submit-form">
    <input type="text" name="username" />
    <input type="submit" />
</form>
  </div>
</form>
  </div>
  <div>
	  <ul> 
      {{#each object}} 
        {{#if_eq completed true}}
        <li>
          <strike>{{name}}</strike>
        </li>
        {{else}}        
        <li>
          {{name}}
        </li> 
        {{/if_eq}}
      {{/each}} 
    </ul>
  </div>
</body>
</html>